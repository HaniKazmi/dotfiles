export ZSH=$HOME/.dotfiles

# Load all zsh files
typeset -U config_files
config_files=($ZSH/*/*.zsh)

for file in ${config_files}
do
  source $file
done

autoload -U compinit
compinit

unset config_files
